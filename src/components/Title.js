import React from "react";

const Title = (props) => {
  return(<div>
    <h2 className="title-container__title">Weather App</h2>
    <p className="title-container__subtitle"> Welcome to the weather app</p>
  </div>
);
}

export default Title;
